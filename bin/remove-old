#!/usr/bin/env ruby
require "sequel"
DB = Sequel.connect ENV["DATABASE_URL"]
puts "Cleaning old sessions..."
DB["delete from pageview where ((website_id = 2 and created_at < now()::date - 5) or (website_id != 2 and created_at < now()::date - 30)) and website_id != 4"]
puts "done."